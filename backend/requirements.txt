fastapi==0.115.6
uvicorn[standard]==0.34.0
anyio==4.15.1
python-multipart==0.0.20
pydantic-settings==2.7.1
Pillow==11.1.0
pillow-heif==0.21.0
filetype==1.2.0
rembg[cpu]==2.0.61
onnxruntime==1.20.1
sqlalchemy==2.0.36
# SQLAlchemy'nin async ORM köprüsü (AsyncSession.close() vb.) bu paketi
# gerektiriyor ama bu sürümde otomatik bağımlılık olarak gelmiyor — pip
# install sırasında "greenlet library is required" hatasıyla açığa çıktı,
# bu yüzden burada ayrıca pinleniyor (brief'te yoktu, bkz. task-1-report.md).
greenlet==3.5.5
asyncpg==0.30.0
alembic==1.14.0
boto3==1.34.162
# Faz 4: Supabase access token doğrulaması (ES256/RS256 + JWKS). `[crypto]`
# eki asimetrik algoritmalar için `cryptography`'yi getiriyor; o da ayrıca
# pinleniyor ki kurulum makineden makineye değişmesin.
pyjwt[crypto]==2.13.0
cryptography==50.0.1
